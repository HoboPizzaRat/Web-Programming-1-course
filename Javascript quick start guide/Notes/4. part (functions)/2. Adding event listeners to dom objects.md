
```javascript
let domObject = document.getElementById("someID");

function getSomeRest(){
	console.log("some action")
}
domObject.addEventListener("click", getSomeRest);
```

you can also add a direct statements into eventlistener like so
```javascript
// the before initializatio code
domObject.addEventListener("click", () => {
	// some other statements
	getSomeRest();
}
);
```