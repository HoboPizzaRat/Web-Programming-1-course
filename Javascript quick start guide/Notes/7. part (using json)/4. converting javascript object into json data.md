we can just use the json data class as it is 

```javascript
let obj = {name: "Mohit", age: 30};
let jsonS= JSON.stringify(obj);

console.log(jsonS);
```

in this case we created a class that has the basic data types in use, take a note that you need to only use the data types that are supported by the json format.


