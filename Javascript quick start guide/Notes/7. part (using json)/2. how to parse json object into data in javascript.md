we dont need to import any json modules whatsoever on javascript, unlike python

we can just call the inbuilt method json.parse

like so
```javascript
let jsonString = '{"name":"Mohit", "age":30}'
let data = JSON.parse(jsonString);

//now we can access datas properties as in any javascript object
let name = data["name"] // getting data from the json object
```