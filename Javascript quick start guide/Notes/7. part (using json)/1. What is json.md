JSON (JavaScript Object Notation) is a widely-used, lightweight data format for representing structured data.

many languages support the conversion between json string data and objects on the given language.
- the text format needs to be converted into objects by some kind of methods
- this allows communication between different platforms and languages extensively(language independent)

example of json data:
```json
{"name":"Mohit", "age":30}
```