A **closure** is created when a function **remembers and can access variables from the scope where it was created**, even after that outer scope has finished executing.


```javascript
function outer() { 
	let message = "Hello"; 
	function inner() { 
		console.log(message); 
	} 
	return inner; 
} 
const myFunction = outer(); 
myFunction(); // "Hello"
```