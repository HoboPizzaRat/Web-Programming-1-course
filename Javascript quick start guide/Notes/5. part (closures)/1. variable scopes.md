scopes are the area where the variable spans

if you define a variable inside a function it is not available outside the function
- until you have defined the same named variable outside the function scope

