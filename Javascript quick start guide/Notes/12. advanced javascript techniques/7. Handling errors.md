## the try catch clause

its pretty much the same idea as on python, though instead of except you use catch

The finally block is always executed


## what error object contains?
error.name
- is the one that contains the general name of the given exception
- typeerror, syntaxerror, ReferenceError etc

error.message
- the general description of where the error occured and why

