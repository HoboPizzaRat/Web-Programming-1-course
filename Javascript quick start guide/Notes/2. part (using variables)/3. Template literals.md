
this is an template literal
```javascript
let variable1 = "some string"
let variable2 = "some other string"

// this is the template literal
// we can combine string data in a concise manner
let combined = `${variable1} ${variable2}`
```