it is a way to iterate through all elements of an array and return a new list where each item has been processed in some way

```javascript
let arr = [1, 2, 3, 4, 4]
let processed = arr.map((item) => item * 2);
// processed = [2, 4, 6, 8, 8]
```