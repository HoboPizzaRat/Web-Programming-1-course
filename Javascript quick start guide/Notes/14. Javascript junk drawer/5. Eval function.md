it is a way to run commands on a javascript console itself as a file. 
- it runs in the same variable scope where it has been defined and can same scope variables inside it
- it can pose a serious security risk

