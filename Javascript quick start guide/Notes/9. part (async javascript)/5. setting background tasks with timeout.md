setting timeouts to execute functions after certain time has passed is possible with setTimeOut function
```javascript

function iAmRealBoy(){
	console.log("delayed action");
}

setTimeOut(iAmRealBoy, 3000);// the secod parameter is on milliseconds

```

