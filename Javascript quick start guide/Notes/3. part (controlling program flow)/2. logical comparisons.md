
The way javascript does logical comparisons compared to python is using different symbols

and is &&
or is ||
not is !

the same order of resolving those combinations apply to javscript

so you can do the followign

```javascript
let value1 = 10
let value2 = 20
let result = (value1 > value2) || (value1 !== value2);
console.log(result)

```