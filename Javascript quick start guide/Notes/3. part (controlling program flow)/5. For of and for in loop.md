examples
```javascript
let arr = [10, 20, 30, 40]
for(let item of arr){
	console.log(item);
}
for(let index in arr){
	console.log(index);
}
```
by using for-of clause you can iterate through all values in the arr

but by using for-in clause you can iterate through keys or indexes of the given object or array
