
```javascript
let example = document.getElementById("example");

// this is how we can remove th example element from dom
example.remove()


// this is how we can create new elements to dom
let element = document.createElemenet("div");
// we must add the element to th document to be visible
let parentElem = document.getElementById("parent");
parentElem.appendChild(element);

```
