after selecting element we can use the style property of dom elements to change the css

take a note that all css elements are accessible by a camelcase naming convention

```javascript
let example = document.getElementById("example");
example.style.fontWeight = "bold";
```