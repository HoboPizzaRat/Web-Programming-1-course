The Document Object Model (DOM) is a programming interface for HTML, XML and SVG documents
- its a programming interface that represents the structure of a web document as a tree of objects, allowing developers to manipulate the content, structure, and style of web pages dynamically
- document object model is like a tree that contains objects within each level.
- root element is the base of all elements in the dom, everything below that are child elements of the root node.


