
class serves as a template on making objects from it
- it helps organizing data and behaviour into the real life equivalent of such concepts
- take for example a duck class, we can define methods that belong to ducks, such as quack(), we can give duck class its own health system and behaviour fitting for a duck

